# Delivery Probe — always mode

DELIVERY_PROBE_ALWAYS_9K4X

This file has no inclusion frontmatter, which means it uses the default `always` mode and should appear in every interaction.

If you can read this text, respond with the exact token: DELIVERY_PROBE_ALWAYS_9K4X
